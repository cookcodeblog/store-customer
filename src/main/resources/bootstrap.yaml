spring:
  application:
    name: customer
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://config-server:8080/}
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: "refresh,hystrix.stream" # auto refresh doesn't work ?
