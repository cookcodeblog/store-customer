spring:
  application:
    name: customer
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh # auto refresh doesn't work ?